package com.example.controller;

//import org.junit.jupiter.api.BeforeEach;
//import org.junit.jupiter.api.Test;
//import static org.junit.jupiter.api.Assertions.*;

public class PlatformTest {

//    private PlatformHandler platformHandler;
//
//    @BeforeEach
//    void setUp() {
//        // Initialize your PlatformHandler or mock objects as needed
//        platformHandler = new PlatformHandler();
//        // Add platforms to the platformHandler as needed for testing
//        // platformHandler.addPlatform(new Platform(...));
//        // platformHandler.addPlatform(new Platform(...));
//    }
//
//    @Test
//    void getIdealLength() {
//        // Ensure the platforms list has at least two elements for testing
//        assertTrue(platformHandler.getPlatforms().size() >= 2);
//
//        double idealLength = PlatformHandler.getIdealLength();
//
//        // Add assertions based on your expectations
//        // For example, if the first platform has a red blob, you might expect its layoutX
//        assertEquals(platformHandler.getPlatforms().get(1).getPerfectionRedBlob().getLayoutX(), idealLength);
//    }
//
//    @Test
//    void getCherrySpawnUpperBound() {
//        // Ensure the platforms list has at least two elements for testing
//        assertTrue(platformHandler.getPlatforms().size() >= 2);
//
//        double spawnUpperBound = PlatformHandler.getCherrySpawnUpperBound();
//
//        // Add assertions based on your expectations
//        // For example, if the second platform has a pillar, you might expect its layoutX minus 35
//        assertEquals(platformHandler.getPlatforms().get(1).getPillar().getLayoutX() - 35, spawnUpperBound);
//    }
//
//    @Test
//    void getCherrySpawnLowerBound() {
//        // Ensure the platforms list has at least two elements for testing
//        assertTrue(platformHandler.getPlatforms().size() >= 1);
//
//        double spawnLowerBound = PlatformHandler.getCherrySpawnLowerBound();
//
//        // Add assertions based on your expectations
//        // For example, if the first platform has a pillar, you might expect its layoutX plus its width
//        assertEquals(platformHandler.getPlatforms().get(0).getPillar().getLayoutX() +
//                platformHandler.getPlatforms().get(0).getPillar().getWidth(), spawnLowerBound);
//    }
//
//    @Test
//    void setStickOffset() {
//        double initialDistance = PlatformHandler.getPlayerNetDistance();
//
//        PlatformHandler.setStickOffset(10);
//
//        double updatedDistance = PlatformHandler.getPlayerNetDistance();
//
//        // Ensure the distance is updated as expected
//        assertEquals(initialDistance + 10, updatedDistance);
//    }
//
//    @Test
//    void getWidth() {
//        // Ensure the platforms list is not empty for testing
//        assertFalse(platformHandler.getPlatforms().isEmpty());
//
//        double width = PlatformHandler.getWidth();
//
//        // Add assertions based on your expectations
//        // For example, if you expect the width of the last platform
//        assertEquals(platformHandler.getPlatforms().get(platformHandler.getPlatforms().size() - 1).getWidth(), width);
//    }
}
